# Raster and vector processing. rasterio ships its own GDAL wheels, which matters
# because there is no system GDAL on the target machine.
rasterio>=1.4.0
numpy>=2.0.0
contourpy>=1.3.0
shapely>=2.0.0
pyproj>=3.7.0
requests>=2.32.0
Pillow>=11.0.0
scipy>=1.14.0
